<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.iu.main.qna.QnaDAO">
	<sql id="searchSql">
	  		WHERE BOARDNUM>0
				<choose>
					<when test="kind=='name'">
						AND BOARDNAME LIKE '%'||#{search}||'%'
					</when>
					<otherwise>
						AND BOARDCONTENTS LIKE '%'||#{search}||'%'
					</otherwise>
				</choose>
	  	</sql>

	<select id="getTotal" parameterType="com.iu.main.util.Pager" resultType="Long">
  		SELECT COUNT(BOARDNUM) FROM QNABOARD
  		<include refid="searchSql"></include>
  	</select>
  	
	<select id="getList" resultType="com.iu.main.qna.QnaDTO" parameterType="com.iu.main.util.Pager">
  		SELECT * FROM 
			(
				SELECT ROWNUM R, B.* FROM 
					(
						SELECT * 
						FROM QNABOARD
						<include refid="searchSql"></include>
						ORDER BY BOARDNUM DESC
					) B
			)
		WHERE R BETWEEN #{startRow} AND #{lastRow}
	</select>
  	
  	

  	
  	
  	</mapper>